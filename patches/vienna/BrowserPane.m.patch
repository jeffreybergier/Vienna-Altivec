--- vienna/BrowserPane.m
+++ vienna/BrowserPane.m
@@ -67,17 +67,26 @@
 @end
 
 @implementation BrowserPane
-@synthesize webPane;
+// @synthesize webPane = webPane; — replaced with manual impl for Tiger compatibility
+- (IBOutlet TabbedWebView *)webPane {
+	return [[webPane retain] autorelease];
+}
+- (void)setWebPane:(IBOutlet TabbedWebView *)webPane_ {
+	id old = webPane;
+	webPane = [webPane_ retain];
+	[old release];
+}
+
 
 + (void)load
 {
-    @autoreleasepool {
+    NSAutoreleasePool *pool = [[NSAutoreleasePool alloc] init];
         if (self == [BrowserPane class]) {
             //These are synonyms
             [self exposeBinding:@"isLoading"];
             [self exposeBinding:@"isProcessing"];
         }
-    }
+    [pool drain];
 }
 
 + (NSSet *)keyPathsForValuesAffectingValueForKey:(NSString *)key
@@ -536,7 +545,7 @@
 	if ( [openDlg runModal] == NSOKButton )
 	{
 		NSArray* files = [[openDlg URLs]valueForKey:@"relativePath"];
-		[resultListener chooseFilenames:files];
+		[(id)resultListener XP_chooseFilenames:files];
 	}
 }
 
